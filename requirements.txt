streamlit>=1.49  # st.navigation, width="stretch"
pandas>=2.2
altair>=5.3

# Only needed to talk to a real Google Sheet. Demo mode does not import them,
# so the app still runs if these fail to install.
gspread>=6.1
google-auth>=2.30
# cryptography 45+ ships no wheel for some platforms and then tries to build a
# Rust/OpenSSL extension from source. Pin below that so a plain pip install works.
cryptography<45

# Exports. openpyxl writes the workbook; fpdf2 draws the printable statement
# and is pure Python, so it needs no system libraries on the deploy host.
openpyxl>=3.1
fpdf2>=2.7

# Reading uploaded documents. pypdf pulls text out of a PDF; Pillow (already a
# Streamlit dependency) shrinks oversized photos so the vision model accepts
# them instead of the upload being refused.
pypdf>=5.0
pillow>=10.0
